# --- Production (Vercel / Netlify) — required at build time ---
# Your Render Flask API URL, no trailing slash
VITE_API_URL=https://creditwise-loan-system-0a7w.onrender.com

# --- Local development (optional) ---
# Leave unset to use Vite proxy: /api → http://127.0.0.1:5000
# Or point at a running local/remote API:
# VITE_API_URL=http://127.0.0.1:5000
